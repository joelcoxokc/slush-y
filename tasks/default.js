module.exports = function (gulp, cb) {

    var options = this.options({
            level: 'two'
        }),
        file  = this.file,
        files = this.files;


        console.log(this)
    // task code ......

    // gulp.src(file.src)
    //     .pipe(someplugin(options))

};