/*
 * Utility
 *   @descriptions  handles and extends all logging, errors and commandline ui utils
 *
 *   _.extends      Chalk
 *   _.extends      inquire
 *   _.extends      fs-extra
 *   _.extends      error-handler
 *   _.extends      logging-handler
 *   _.extends      ui-loggind-handler
 */

/**
 * Base             Controller
 *   @desc          handles all gloabl controller functions
 */

/**
 * NameBase         Controller
 *   @desc          handles all name based controller functions
 */

/**
 * ArgsBase         Controller
 *   @desc          handles args based controller functions
 */

/**
 * Base             Class
 *   @desc          wrapper for a  controllers and utility
 *   _.extends      Utility
 *   _.extends      Base.controller
 */


/**
 * NameBase         Class
 *   @desc          handles controller functions that deal with name based commands
 *
 *   _.inherit      Base
 *   _.extends      NameBase.controller
 */

/**
 * ArgsBase         Class
 *   @desc          handles controller functions that deal with args based commands
 *
 *   _.inherit      Base
 *   _.extends      ArgsBase.controller
 */


/**
 * Slushy
 *   @description   Front interface for all extended components
 *
 *   @wrap          Base      as  Slushy.Base
 *   @wrap          NameBase  as  Slushy.NameBase
 *   @wrap          ArgsBase  as  Slushy.ArgsBase
 */




/**
 * defauult
 * @description     grabs defaults from operating system's .giticonfig
 */

