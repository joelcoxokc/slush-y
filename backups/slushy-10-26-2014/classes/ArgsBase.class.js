;(function(){

  'use strict';

    var _        = require('lodash');
    var util     = require('util');
    var Base     = require('./Base.class');


    function ArgsBase (param) {

      // Base.apply(this, arguments);

      var __this = this;

    }

    // util.inherits(ArgsBase, Base);

    // _.extend(ArgsBase.prototype, require('../controllers/ArgsBase.controller'))

    ArgsBase.prototype.publicMethod = function() {};


}).call(this);